/*
	1. main 안에 void 넣는 이유
	2. main의 return 타입이 int인 이유

	1. & 사용하여 주소값을 갖는 변수에 값을 부여할 수 있는지?


	- 자식 클래스에서 부모 클래스의 기본 생성자 호출 없이는 사용할 수 없는 것인지?

	- endif 뒤에 관례적으로 달아주는 주석에는 주석 형태에 대한 규칙이 존재하는지?

	- 당장에 상속받지 않는 함수들에도 virtual을 습관적으로 명시해주면 나중에 혹여나
	  상속을 하여 사용하게 되었을 경우, virtual이 이미 명시적으로 선언되어 있기 때문에
	  상속시 virtual을 다시 선언할 수고를 덜어낼 수 있으니 virtual을 습관적으로 그냥 달아놓고
	  사용하면 안되는 것인지? 성능상에 문제가 있는것인지?

	- 소멸자에 virtual 함수가 명시적으로 선언되어 있지 않았을때의 문제점?

	- 한 프로젝트 안에 여러개의 main함수가 존재하면 안되는 이유?

	- 유니폼 초기화란?
*/